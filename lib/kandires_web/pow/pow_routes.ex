defmodule KandiresWeb.Pow.Routes do
  use Pow.Phoenix.Routes
  alias KandiresWeb.Router.Helpers, as: Routes

  # @impl true
  # def after_sign_out_path(conn), do: Routes.some_path(conn, :index)

  # def after_sign_out_path(conn), do: Routes.some_path(conn, :index)
end
